// Metrics service placeholder for Node.js compatibility
// Actual metrics are handled by the Python backend

export const metricsService = {
  recordExecution: () => {
    // Delegated to Python backend
  },
  recordAPICall: () => {
    // Delegated to Python backend  
  }
};
