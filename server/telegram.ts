// Telegram service placeholder for Node.js compatibility
// Actual Telegram bot functionality is handled by the Python backend

export const telegramService = {
  sendMessage: async () => {
    // Delegated to Python backend
  }
};
